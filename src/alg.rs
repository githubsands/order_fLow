pub struct AlgorithmRunner {
    algorithm Fn(&str)
}

impl AlgorithmRunner {
    pub fn new() -> Self {}
    pub fn consume_tick(tick: &tick) -> Result<> // this is some type of
    pub fn run() -> Result<>
    pub fn shutdown() -> Result<>
}
